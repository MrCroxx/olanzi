# 05 · 验证记录
> 🌐 [English](05-verification-log.md)

> 所有实测数据的留档，**包括失败的尝试**。
> 结论见 [02-vibekey-protocol.md](02-vibekey-protocol.zh.md)，过程见 [04-methodology.md](04-methodology.zh.md)。

> 📚 文档集：[README](../README.zh.md) · [01 职责边界](01-ulanzi-studio-scope.zh.md) · [02 协议](02-vibekey-protocol.zh.md) · [03 工具手册](03-tool-manual.zh.md) · [04 逆向方法论](04-methodology.zh.md) · **05 验证记录** · [10 输入运行时](10-input-runtime.zh.md)

---

## 验证环境

| 项 | 值 |
|---|---|
| 日期 | 2026-09-21 |
| 系统 | macOS（Apple Silicon） |
| Ulanzi Studio | 3.3.9 |
| Vibe Key 固件 | 4.4.2 |
| 设备序列号 | `202606031150` |
| 工具 | `vibekey.py`（本项目） |

> 全部验证都在 **Ulanzi Studio 已退出** 的状态下进行（除特别标注）。

---

## 1. 设备身份读取

**命令**：`python3 vibekey.py --probe -t 3`

| 查询帧 | 设备回复（解密后） | 解读 |
|---|---|---|
| `06 03 0a 01` | `01` | 设备在线 |
| `01 04 0b 01` | ⟨16 字节，已脱敏⟩ | flashId，前 7 字节 ASCII = `AP53002`（型号前缀） |
| `01 01 0b 01` | `0a 00` + ⟨14 字节，已脱敏⟩ + `07 01` + ⟨8 字节，已脱敏⟩ | SN = **`<REDACTED>`**（分两包） |
| `01 04 04 01` | `41 00 00 28 28 01 04 04 02 26 08 13 20 30` | 固件 **4.4.2** |
| `06 02 03 01` | `41 00 00 16 28 00 04 04 02 26 08 13 20 30` | dongle **4.4.2** |
| `01 01 02 01` | `dd 0d 1e 00 f1 01` | `0x0ddd` = 3549 mV |
| `01 01 90 01` | `00 64 00 64` | 双麦降噪：各 `低=0 高=100` |
| `01 0b 88 01` | `00 00 02 07 02 \| 0a 02 07 02 02 \| 0a 02 07 02 01 \| 0a 02 07 02 00` | 指示灯参数（疑似 3 组 + 头） |
| `01 06 31 01` | 全 0 | 全部按键功能（未配置） |
| `01 06 21 01` | 全 0 | AI 按钮功能（未配置） |
| `01 06 40 01` | 全 0 | 马达强度（未配置） |
| `01 01 34 01` | 全 0 | 旋钮开关（未配置） |
| `01 0b 89 01` | 全 0 | Hooks 模式（关） |
| `01 01 41 01` | 全 0 | 硬件版本（未配置） |

**结论**：设备在**完全没有 Studio** 的情况下可以正常应答。 ✅

---

## 2. 按键映射实验

### 实验 1 —— 自由按压（13:27，失败但产生了发现）

用户自由操作约 40 秒，捕获 47 次按下：

| keycode | 次数 | 时段 |
|---|---|---|
| `0x2A` Backspace | 10 | 13:27:22.664 – 25.569 |
| `0x46` PrintScreen | 4 | 13:27:25.976 – 29.175 |
| `0x01` ErrorRollOver | 12 | 13:27:30.535 – 37.020 |
| `0x28` Enter | 10 | 13:27:31.394 – 37.319 |
| `0x29` Esc | 11 | 13:27:32.224 – 38.088 |

**结果**：无法建立映射（无标注）。
**但发现了时长规律** —— `0x2A` 全部 4–7 ms，其余全部 64–205 ms。

### 实验 2 —— 自由按压（13:28）

| 时间 | 事件 | 时长 |
|---|---|---|
| 13:28:27.836 | `0x46` | 129 ms |
| 13:28:29.480 | `0x46` | 174 ms |
| 13:28:30.375 | `0x46` | 120 ms |
| 13:28:31.959 | `0x01` | **1855 ms** |
| 13:28:34.080 – 34.845 | `0x01` ×6 | 80–139 ms |

**结果**：仍无法定映射。但 `0x01` 出现 1855 ms 长按，说明它**确实是一个控件**。

### 实验 3 —— 受控顺序（13:29）✅ **成功**

**操作序列**（每步间隔 3 秒）：
> 按下旋钮 → 键1 → 键2 → 键3 → 右拧×3 → 左拧×3 → 长按旋钮

**捕获结果**：

| 时间 | 事件 | 时长 | 对应动作 |
|---|---|---|---|
| 13:29:20.180 | `0x46` PrintScreen | 300 ms | ① 按下旋钮 |
| 13:29:21.095 | `0x01` ErrorRollOver | 320 ms | ② 键 1 |
| 13:29:21.980 | `0x28` Enter | 400 ms | ③ 键 2 |
| 13:29:22.760 | `0x29` Esc | 260 ms | ④ 键 3 |
| 13:29:26.079 | `0x4F` RightArrow | 4 ms | ⑤ 右拧 #1 |
| 13:29:26.444 | `0x4F` RightArrow | 5 ms | ⑤ 右拧 #2 |
| 13:29:26.985 | `0x4F` RightArrow | 6 ms | ⑤ 右拧 #3 |
| 13:29:29.414 | `0x2A` Backspace | 4 ms | ⑥ 左拧 #1 |
| 13:29:29.831 | `0x2A` Backspace | 0 ms | ⑥ 左拧 #2 |
| 13:29:30.215 | `0x2A` Backspace | 10 ms | ⑥ 左拧 #3 |
| 13:29:34.459 | `0x46` PrintScreen | **1220 ms** | ⑦ 长按旋钮 |

**7 个动作 ↔ 7 组事件，顺序、数量、类别全部吻合。** ✅

### 实验 4 —— 独立复核（13:36）

用户按自己的习惯再操作一次：

```
13:36:28.960 按键   ⌨ 旋钮 按下    PrintScreen    178 ms
13:36:32.822 旋钮   ⟳ 旋钮 ← 左拧   Backspace        2 ms
13:36:33.778 旋钮   ⟳ 旋钮 → 右拧   RightArrow       3 ms
13:36:35.209 按键   ⌨ 键 1 (上)    ErrorRollOver  132 ms
13:36:36.164 按键   ⌨ 键 2 (中)    Enter          147 ms
13:36:36.908 按键   ⌨ 键 3 (下)    Esc             90 ms
```

**与实验 3 完全一致。** 映射确认。

### 交叉验证：设备端配置表

从设备读出的官方配置（见 §3）与实验推出的映射**逐字节吻合**：

```
index 0..5 →  01  28  29  46  4f  2a
实验结论   →  01  28  29  46  4f  2a   ✅
```

---

## 3. 可编程按键表

### 3.1 读取全部配置

**命令**：`send_frame(01 06 50 01 <i>)` for `i` in 0..7

```
index 0 → 81 06 50 11 00 01 01 02 01 00 00 ...
index 1 → 81 06 50 11 01 01 01 02 28 00 00 ...
index 2 → 81 06 50 11 02 01 01 02 29 00 00 ...
index 3 → 81 06 50 11 03 01 01 02 46 00 00 ...
index 4 → 81 06 50 11 04 01 01 02 4f 00 00 ...
index 5 → 81 06 50 11 05 01 01 02 2a 00 00 ...
index 6 → 81 06 50 11 06 00 00 00 00 00 00 ...     ← 未使用
index 7 → 81 06 50 11 07 00 00 00 00 00 00 ...     ← 未使用
```

**解析**（对照 lldb 反汇编出的 setter 格式）：

```
81 06 50 11 | index | 01 | num | ⟨类型|sign<<7⟩ ⟨键码⟩ ×num
              pt[4]   pt[5] pt[6]   pt[7]        pt[8]
```

### 3.2 ⭐ 写入验证（端到端闭环）

**目标选择**：`index 0`（键 1），原值 `0x01` = 无效码 —— **改了不会损坏任何功能**。

| 步骤 | 报文 / 结果 |
|---|---|
| 1. 读原值 | `81 06 50 11 00 01 01 02 01 00` |
| 2. 写入 | `→ 01 06 50 04 00 01 01 02 68` |
| 3. 写回复 | `← 81 06 50 14 00 01 01 02 68 00` （`access=0x14` = 写确认） |
| 4. 读回 | `81 06 50 11 00 01 01 02 68 00` ✅ **已持久化** |
| 5. **按真键** | **捕获到 `0x68` = F13，185 ms** ✅✅✅<br>← [原始证据第 282 行](evidence/2026-09-21-key-reprogram.log) |
| 6. 还原 | `→ 01 06 50 04 00 01 01 02 01` → 读回 `[02, 01]` ✅ |

**同时确认其余 5 个键未受影响**：

```
index 1 : 01 01 01 02 28 00    ← 0x28 Enter，未变
index 2 : 02 01 01 02 29 00    ← 0x29 Esc，未变
index 3 : 03 01 01 02 46 00    ← 0x46，未变
index 4 : 04 01 01 02 4f 00    ← 0x4f，未变
index 5 : 05 01 01 02 2a 00    ← 0x2a，未变
```

**结论**：`类型=0x02` 时，第二个字节就是设备会上报的 HID 键码。 ✅

### 3.3 命令行的最终形态

```bash
$ python3 vibekey.py --keys
  设备端按键配置（index 0-5 = 六个控件）

  index     控件               类型           键码
  ──────────────────────────────────────────────────────────
  0         键 1 (上)          按键           0x01 (无效码/未配置) 0x01
  1         键 2 (中)          按键           Enter 0x28
  2         键 3 (下)          按键           Esc 0x29
  3         旋钮 按下            按键           PrintScreen 0x46
  4         旋钮 → 右拧          按键           →右方向键 0x4F
  5         旋钮 ← 左拧          按键           Backspace 0x2A
```

---

## 4. 厂商通道行为

### 4.1 Studio 退出后，按键期间厂商通道是否发事件？

**实验**：`vibekey.py --poll 2 -t 600`，用户按多个键。

**结果**：按键时段内厂商通道**只有心跳**，**零条按键事件**。

```
13:28:37.660 厂商 → 通知/心跳 ctr=2182346706399   cmd=0x0B flags=0 len=63
```

**结论**：**脱离 Studio 后，按键只走标准 HID（接口 2），不碰私有通道。** ✅

### 4.2 Studio 运行时呢？（对照）

`cap3.log`（Studio 运行中）11:55:37–43 连续 6 次按"键 1"：

```
11:55:37.924 iface2 KEYBOARD keys=['ErrorRollOver']
11:55:38.999 iface2 KEYBOARD keys=['ErrorRollOver']
11:55:40.120 iface2 KEYBOARD keys=['ErrorRollOver']
11:55:41.140 iface2 KEYBOARD keys=['ErrorRollOver']
11:55:42.583 iface2 KEYBOARD keys=['ErrorRollOver']
11:55:43.444 iface2 KEYBOARD keys=['ErrorRollOver']
```

同一时段**接口 3 无任何帧**。

**结论**：即使是 Studio 在跑，**按键也走标准 HID**。
（`deviceKeyEvent` 出现在历史日志里，但它**不是按键的必要通路**。）

### 4.3 历史日志中的 deviceKeyEvent

从 62 MB 解码日志统计（4 天数据，共 3893 条）：

| index | 条数 |
|---|---|
| 0 | 518 |
| 1 | 230 |
| 2 | 38 |
| 3 | 142 |
| 4 | 645 |
| 5 | 2320 |

样例：

```json
{ "status" : 1, "access" : 2, "type" : "deviceKeyEvent", "index" : 3 }
```

> `index 0..5` 与 §3 的控件编号体系一致，说明它是**同一套编号**。
> 但触发条件未完全确定（见"未解问题"）。

### 4.4 设备离线时的现象（最终回归时发现）

跑最终回归时发现 `--keys` 六个控件全部 `(无回复)`。排查结论：

**dongle 活着，Vibe Key 本体没开机。**

| 查询 | 回复 |
|---|---|
| `06 03 0a 01` 设备在线状态 | `06 03 0a 11` **00** `…` ← status = **0x00 离线** |
| `06 02 03 01` dongle 版本 | `06 02 03 11 41 00 …` ✅ 正常 |
| 其余 15 条（全部 `cmd=0x01`） | ❌ 无回复 |

**规律**：`cmd=0x06` 由 dongle 处理，`cmd=0x01` 由设备本体处理（走无线链路）。
本体关机时只有 `0x06` 会回。

> 这不是故障，是设备的正常休眠/关机状态。已给 `vibekey.py` 加上离线提示。

#### ⚠️ 一次错误诊断（留档）

用户开机后，`--keys` 恢复正常（读到全部 6 个键），但 `--probe` 仍报"设备离线"。
我一度据此认为设备仍处于休眠。**这个判断是错的** —— 真实原因是我自己刚写的代码：

```python
    @staticmethod                    # ← 没有 self
    def _describe(cmd, pt):
        if is_reply and cmd == 0x01:
            self.device_replied = True    # ← NameError
```

异常在 **ctypes 回调里被静默吞掉**（只打一行 `Exception ignored`），
于是回复行根本没打印 —— 症状与"设备关机"完全一致。

**为什么 `--keys` 不受影响**：它设了 `self.hide = True`，在调用 `_describe` **之前**就 return 了。

**教训**：判断离线不能只看"有没有回复"，要看**原始明文**。
真离线时 `06 03 0a 11` 会回但 status 是 `00`；代码 bug 则**一行都不打印**。

---

## 5. 失败的尝试（留档）

| 尝试 | 结果 | 原因 |
|---|---|---|
| 用 `IOHIDManagerOpen` 一次性打开 | ❌ `0xE00002C5` | 自己的抓包进程占着设备 |
| 在 DSH 里跑抓包，同时用户跑 | ⚠️ 可用但会互相干扰 | 后来确认**非独占打开可以并存** |
| 自由按压建立映射（实验 1、2） | ❌ 无法对齐 | 缺标注 |
| 按"4 键 + 旋钮"模型推理 | ❌ 全错 | 实际是 **3 键** |
| `objdump --start-address` 精确定位 | ❌ 输出全量 | LLVM objdump 对 Mach-O 支持有问题 |
| 自动提取 setter 的 opcode | ❌ 得到 `01 00 00 00` | 提取脚本未覆盖该模式，需手工 lldb |
| 用 `grep` 搜 62 MB 日志 | ❌ 报错 | `maximum repetition exceeds 255`，改用 Python |
| 事后对齐日志与抓包时间 | ❌ 对不上 | xlog **延迟 flush**（当天日志最后写入 11:21，抓包在 11:44） |
| 后台跑工具让用户看输出 | ❌ 用户看不到 | 输出没显示在用户屏幕上，应在用户终端跑 |
| 用 iTerm2 跑但没输入监控权限 | ❌ 按键生效却抓不到 | `0xE00002E2`，且程序当时**静默跳过**了 |

---

## 6. 未解问题

| 问题 | 现状 | 下一步 |
|---|---|---|
| 组合键（`num > 1`） | 未测 | 试写 `[(0x02,0xE0),(0x02,0x06)]`（推测 = Ctrl+C） |
| `类型 = 0x03`（系统/多媒体） | 未测 | 从解析器 `isSysCtrl` 分支推范围 |
| `sign` 位（bit 7）语义 | 未测 | 观察 Studio 写入的组合键 |
| `deviceKeyEvent` 触发条件 | 未定 | 可能是"按键功能被配置过"或某个握手 |
| 指示灯参数字段布局 | 部分 | `00 00 02 07 02 \| 0a 02 07 02 02 \| …` 三组含义未定 |
| AI 状态 → 灯的数值映射 | 未解 | lldb 断点 `+[MessageHelper setDeviceAIButtonFuncMessage:]` |
| 电源键长按 | 未测（有风险） | 非必要 |
| `index 6/7` 的用途 | 未知 | 可能是其他型号的控件（如 4 键版本） |

---

## 7. 原始数据位置

| 数据 | 路径 |
|---|---|
| **可编程按键表的原始证据** | [evidence/2026-09-21-key-reprogram.log](evidence/2026-09-21-key-reprogram.log) |
| 事件日志（工具自动写） | `/tmp/vibekey-events.log` |
| 会话抓取 | `/tmp/vk.log`、`vk2.log`、`vk3.log`、`vk4.log`、`vk5.log` |
| HID 抓包（含 Studio） | `~/ulanzi-re/raw/cap2.log`、`cap3.log` |
| 62 MB 解码日志 | `~/ulanzi-re/raw/logs_decoded.txt` |
| 命令表 | `~/ulanzi-re/raw/kwdm_message_builders.txt` |
| kwdm 反汇编 | `~/ulanzi-re/raw/kwdm_arm64_disasm.txt` |

---

## 8. 心跳调查（2026-09-21）

[CONFIRMED] 只使用厂商接口，先测量 60.005 秒零请求空闲，再测量 60.005 秒内每两秒一次的 30 次 Hooks 查询。两个窗口结束均回复 `06 03 0a 11 01 00 00 00`，没有通知，也没有回调异常。未发送配置写入。

[CONFIRMED] LLDB 找到 Studio 独立心跳：`06 01 23 00 01` 加 59 个零字节，约每秒发送一次。旧 `--poll` 读取 Hooks 模式，不发送该帧。稍后的 Studio 心跳测试尝试起始即得到 `06 03 0a 11 00 00 00 00`，在发送心跳前终止。此前六个控件的读取未完成，因此检查器已改为先验证在线状态。

[CONFIRMED] 用户报告物理唤醒后重试仍返回离线。单独诊断成功提交 10 帧官方心跳，穿插在线查询、间隔约两秒；所有状态仍为离线，六键读取仍未完成。没有通知或回调异常。这不是在线起点、一秒间隔的心跳对照。

[INFERRED] 稍后离线可能是休眠，但没有测量发生时刻与原因。在这些尝试中，专用心跳窗口及按键配置前后对照未完成。用户操作、供电条件与更长空闲时间未受控。这些测量不能证明防休眠效果，也不能根据无线在线状态证明指示灯保持清醒。

[CONFIRMED] 无线连接恢复后，真实工作台读取全部六个控件，将键 1 从 `0x01` 写为 F13（`0x68`），收到确认且设备回读匹配，再恢复为 `0x01`。最终六个控件配置全部与原快照一致。[往返证据](evidence/2026-09-21-workspace-roundtrip.log)通过回读验证配置写入结果，不代表已验证物理按键事件。

[CONFIRMED] 随后通过 HTTP 观察运行中的本地服务 **90.019 秒**，以约一秒间隔取得 91 个样本。每次均报告 `online=true`、`connected=true`，Studio 心跳开启、间隔一秒，且没有错误。观察到 `lastSent` 在 86 对相邻样本之间推进；采样时该时间戳距当前最多 1.001 秒。采样与心跳调度不同步，因此相邻时间戳相同不代表漏发心跳。窗口前后通过 `POST /api/refresh` 读取全部六个控件，配置完全一致。服务继续运行。见 [90 秒观测日志](evidence/2026-09-21-workspace-heartbeat90.log)。

这是成功的短时运行验证，不是隔离变量的防休眠实验：服务同时每两秒查询在线状态，前后读取键位也可能影响清醒状态。HTTP 状态读取只是观察现有服务，窗口内没有另开 HID 客户端或写入配置。长期休眠因果关系与物理按键事件仍未验证。

见 [07 心跳调查](07-heartbeat-investigation.zh.md)、[对照日志](evidence/2026-09-21-keepalive-comparison.log)和[原始反汇编](evidence/2026-09-21-studio-heartbeat-disassembly.log)。不要将缺少报文当作关机证据；应读取原始状态字节。


---

## 9. 原生 App 本地复查与修复（2026-09-21）

### 9.1 首次初始化失败仍发送心跳

[CONFIRMED] 在另一台电脑完成的 PR #2 于本机启动后，接收器与设备在线，顺时针旋转控件 `index 4` 回读为 `type=0x03`、`code=0x04`，其余五个控件为出厂值。本机尚无 `host-keymap.json`；应用报告不支持该类型，未创建本机映射，全部动作编辑不可用，但仍持续发送 Studio 心跳。这次复查未改写设备键位，也未通过实体按键验证输入失效。

[INFERRED] 未初始化时厂商事件不会被主机桥接转发；结合 [07 §4](07-heartbeat-investigation.zh.md#4-心跳改变按键上报路径2026-09-21) 已有实测证明心跳会停止标准 HID 直出，该代码路径会使普通设备输入不可用。此判断基于本次初始化与心跳观察、代码检查及此前硬件证据，不把它写成本次实体按键实测。

修复实现将 Studio 心跳改为默认停用，只有有效本机映射、设备已连接且在线、输入监控与辅助功能权限就绪且输入未暂停时才启用。暂停心跳后仍保留只读查询。查询等待期间也检查权限和睡眠，正常在线查询完成前保留上次确认状态，避免中断按住动作。初始化遇到不支持的设备动作时不会自动换成默认值；界面可创建出厂键位草稿或导入配置，经用户显式保存后仅持久化本机映射。损坏的本机文件仍保留，须修复并重启，不能由草稿覆盖。本节描述的设备键位初始化与手动创建草稿恢复属于历史中间行为，已由 §9.8 取代；心跳门控与损坏文件保护仍保留。

### 9.2 保存结果与配置集合迁移

代码复查同时发现两项 P2 问题，并作以下修复；初步修复的自动回归已通过。

| 问题 | 触发条件与原行为 | 修复实现 |
|---|---|---|
| 保存完成识别 | 保存排在刷新或重连之后时，先前任务的忙碌状态结束可能被当作保存完成，提前清除待确认状态并误报失败。 | 每次保存使用请求 ID；服务返回匹配的成功或失败结果，界面只完成相同请求，保留保存期间的新草稿编辑。 |
| 配置集合整体解码 | 配置列表中一个损坏或不支持的条目使整份数组解码失败，其余有效配置也无法载入。 | 逐项解码与迁移，载入有效条目；失败项所在的完整原始数据备份到 `nativeHostProfilesRecoveryBackups`，后续列表编辑不覆盖恢复副本。 |

### 9.3 初始化失败时隐藏权限提醒

用户进一步指出 Mac App 可能缺少权限却没有明显提醒。代码检查确认，原来 `VendorKeyBridge` 仅在配置与设备均就绪时报告缺少权限，页底授权按钮又要求桥接已启用；因此本机配置为空或设备离线时，输入监控和辅助功能权限不足可能被隐藏。修复将权限诊断与配置、在线状态分开，即使尚未初始化也显示缺少的权限及相应授权入口。这一权限提示补丁的自动回归与真实应用界面复验已通过，范围见下节。用户随后要求缺少权限时只显示欢迎页，不进入配置工作区或设置界面；因此进一步改为主窗口内部的权限欢迎页，提供两项授权引导、对应系统设置按钮与 App 位置入口，权限就绪后自动进入工作区，撤权后回到欢迎页，演示模式豁免。它通过替换主窗口内容实现，不另开弹窗、窗口或 sheet，也不堆叠初始化错误与页底提示。

[CONFIRMED] 使用 `codesign` 核实，本次运行的应用采用钥匙串中唯一的 Apple Development 身份签名，应用标识为 `com.mrcroxx.olanzi`，不是临时签名。签名身份稳定与系统权限已获准是两回事；此检查不证明输入监控或辅助功能已授权。

### 9.4 验证边界与待验项

[CONFIRMED] 修复前本次本地检查通过了既有 120 项原生测试、84 项 Python 测试和 3 项 JavaScript 测试；这些通过结果未覆盖上述首次初始化、保存排队与混合有效性配置集合场景。

[CONFIRMED] 欢迎页调整前的修复基线中，131 项原生测试全部通过，`make build` 成功，`codesign` 严格验证通过，构建使用钥匙串证书签名。本次真实设备在线、初始化仍因不支持的动作失败时，界面已显示心跳“未运行”；“使用出厂键位创建本机配置”按钮可点击，生成草稿后控件可编辑。该界面检查没有保存本机配置，也没有改写设备键位，不能据此声称主机转发或实体快捷键已生效。

[CONFIRMED] 欢迎页调整前，真实应用的辅助功能界面树与截图同时显示 `type=0x03` 初始化错误、`权限不足：按键转换需要输入监控与辅助功能权限，心跳已暂停。` 以及 `打开输入监控设置` 按钮。设备在线，心跳未运行。最终重启后仍保留原来的未初始化状态；此前创建出厂键位草稿的检查没有落盘。

[CONFIRMED] 欢迎页实现后，133 项原生测试全部通过，新增覆盖两项权限都获准才进入工作区、撤权后返回欢迎页且保留草稿、演示模式绕过权限门控。上述同时显示错误与页底授权入口的实测是欢迎页调整前的中间调试观测。欢迎页版本的 `make build` 已成功，继续采用钥匙串证书签名。

[CONFIRMED] 最终欢迎页的应用界面树与截图均确认只有单个 Olanzi 标准主窗口。正文显示 `欢迎使用 Olanzi`、两项权限待开启、`打开输入监控设置` 与 `显示 App 位置` 入口；无键位、设备或配置导航，无初始化错误或保存页底栏，也没有额外弹窗或 sheet。页面布局完整，验证范围为权限不足时主窗口内部的欢迎界面。

实体按键转发、双击和长按效果仍待授予权限及有序物理操作验证；系统撤权和睡眠的实体行为也需另测。自动回归、界面观察与实际输入效果分别记录，不得将测试替身的成功当作系统快捷键已触发。

### 9.5 授权后的独立状态轮询

[CONFIRMED] 随后系统设置的输入监控列表中，`Olanzi.app_Toggle` 为开启，而当时运行的旧应用欢迎页仍显示两项权限待开启。这确认了设置界面与应用显示不一致，不能据此认定某个权限 API 存在缓存，也不能把设置中的条目开关当作当前运行进程已获准的证据。

修复增加 App 每秒一次、无弹框的独立权限检查，定时器运行于主 run loop 的 common 模式，不依赖设备工作线程或窗口重新激活。界面单独保存最新轮询状态，防止较旧设备快照覆盖；仅在状态变化时通知服务强制复查，并自动切换欢迎页与工作区。停止或销毁模型会取消定时器，演示模式不调用真实检查。

界面与工作线程共用 `InputPermission.currentStatus()` 所采用的权限判据：`CGPreflightListenEventAccess()` 为真时输入监控可用，为假时仅在 `IOHIDCheckAccess()` 未知时保留未知，其他情况为不可用，HID 已获准不能遮掩 CG 监听预检拒绝。这一中间版本的辅助功能仍使用 `CGPreflightPostEventAccess() && AXIsProcessTrusted()`，前者为假时不调用后者，原意是保留首次授权保护；后来证实该条件过宽，已在 §9.6 纠正。只在原始权限结果变化时记录诊断，不读取键盘内容。

[CONFIRMED] 该轮修改后 136 项原生测试通过，包括权限检查冲突、撤权、短路保护和独立界面状态更新。该版本的 `make build` 与严格签名验证已通过。

[CONFIRMED] 重建并重启后的真实应用界面树显示输入监控已开启、辅助功能待开启，主按钮自动指向辅助功能设置，并显示每秒自动检测的说明。当前签名进程的原始诊断为 `HID=0, CGListen=true, CGPost=false, accessibility=false`。这是重启后的状态观察，不证明任一 API 有缓存，也不证明不重启时的真实授权或撤权切换已经完成。该观察中的 `accessibility=false` 是短路计算结果，不能当作直接读取 AX 后的拒绝；后续原位检查在 §9.6 纠正了这一解释。实时切换由定时器回归测试覆盖，按键转发与手势效果未在本轮实测。

### 9.6 事件合成预检不能代替辅助功能信任

[CONFIRMED] 用户已在系统设置开启辅助功能，应用仍显示待开启。使用 LLDB 附加当时仍运行、保留原签名的 Olanzi 进程，在 `@import ApplicationServices` 后分别调用得到 `AXIsProcessTrusted()=true`、`CGPreflightPostEventAccess()=false`、`CGPreflightListenEventAccess()=true`，随后正常 detach。测量过程中未修改系统权限，也不是重启后才读取结果。

这直接证明当时的辅助功能误报来自代码中的 `CGPost && AX`：前者为假使后者根本没有被调用。不能把该状态归因于用户未授权、签名错误或已确认的系统缓存。教训是分清事件合成能力预检与辅助功能客户端信任，不把未执行的查询记录成真实拒绝，也不把最初授权流程的保护扩大为始终阻止 AX 检查。

修复使用 `trust = (listening || posting) ? AXIsProcessTrusted() : nil`，辅助功能状态为 `trust == true`；只有监听和事件发送预检都为假时才跳过 AX，保留最初保护。日志明确区分 `AX=skipped`、`AX=true`、`AX=false`。新增回归覆盖 CGPost 持续为假时 AX 授予和撤销的变化，以及输入监控拒绝、事件发送预检为真时能读取 AX 但输入仍不可用。

[CONFIRMED] 该修复后 138 项原生测试全部通过，`make build` 沿用钥匙串 Apple Development 证书并通过严格签名验证。重建并重启后的真实应用直接进入配置主窗口，不再显示欢迎页或权限不足；当前进程日志为 `HID=0, CGListen=true, CGPost=true, AX=true`。设备仍因原有多媒体类型 `0x03` 不能自动初始化，本次未写设备，也未创建本机配置。

新进程中的 CGPost 为真不改变此前原运行进程测得 CGPost 为假、AX 为真的事实，也不能用来断言缓存原因。AX 为真只确认辅助功能信任；实际按键输出及手势效果仍需分别验证。


### 9.7 预检拒绝时验证实际键盘监听能力

[CONFIRMED] 用户再次开启输入监控后，系统设置开关为开启，应用仍未勾选。LLDB 在同一原运行进程 PID 78712 中测得 `IOHIDCheckAccess(Listen)=1`（denied）、`CGPreflightListenEventAccess()=false`、`CGPreflightPostEventAccess()=false`、`AXIsProcessTrusted()=false`。随后创建 session 层、head insertion、listen-only、仅 key-down/key-up 的事件 tap 成功，`CFMachPortIsValid` 与 `CGEventTapIsEnabled` 均为真。探针立即 invalidate 并 release，没有调度到 run loop；再次读取 CGListen 仍为假。

这份原位证据确认预检结果与可创建的键盘监听能力不一致，不证明任一 API 的具体缓存机制，也不证明设备键位转发已经成功。探针仅检验能力，没有读取或记录键盘事件。由于键盘 tap 能力也可能受辅助功能授权影响，不能把创建成功写成系统设置中单独输入监控开关的直接读数。

本地 Digger 的当前 `PermissionChecker.swift` 仅直接读取 `AXIsProcessTrusted()`；`WelcomeViewModel` 每秒重新检查，状态变化才更新界面，显示窗口时立即刷新、关闭时停止。其历史 `tapCreate` 用于长期 ForceClick 监听，不是权限探针，因此不把新方案归称为 Digger 已验证的输入监控实现。

本轮保留每秒独立轮询，但在 CGListen 为假时增加上述短暂能力探针；输入可用性取预检或有效探针成功。辅助功能改为每次无条件读取 AX，取消 §9.5/§9.6 的短路条件，原始 HID、CGListen、`ListenProbe`、CGPost、AX 结果分别记录。§9.5/§9.6 保留为中间实现与失败尝试的历史，不再代表当前判据。

[CONFIRMED] 加入能力探针后的 139 项原生测试通过，`make build` 与严格签名验证通过，继续使用同一钥匙串 Apple Development 证书。

[CONFIRMED] 为建立新进程的未授权基线，退出旧应用后关闭输入监控，再于 22:45:32 启动新版 PID 81521。日志为 `HID=1, CGListen=false, ListenProbe=false, CGPost=false, AX=false`，界面两项待开启。用户自行开启输入监控并报告 `已开启，自动打勾了`；同一 PID 于 22:45:59 记录 `HID=1, CGListen=false, ListenProbe=true, CGPost=false, AX=false`，22:46:05 的 AX 变为真，其余值保持不变。已核实 PID 未改变，真实应用界面自动进入配置主窗口，期间没有退出或重启。

这次完成了同一运行进程内输入监听能力就绪和辅助功能授权后的真实实时界面切换，不能再只归为重启后的状态观察。完整运行中撤权循环尚未测试，实际按键输出与手势效果未验证；设备原有类型 `0x03` 导致未初始化的状态仍保留，未改写设备配置。


### 9.8 分离本机默认配置与可选设备键位导入

用户指出仍存在架构错误：设备旧动作不受支持，不应阻止独立本机动作的配置。已观察到的 `type=0x03` 描述的是设备旧回退键位，并非应用配置的必需来源。§9.1 的设备键位初始化方案及截至 §9.7 记录的未初始化状态均为历史行为，由以下规则取代。

- 工作线程优先加载已有本机配置；文件不存在时发布 `hostConfigurationMissing=true`，不从设备生成映射，也不自动写文件。
- 本机文件缺失时，应用直接提供 `HostKeymap.defaultKeymap` 可编辑草稿。权限就绪后，用户可离线配置；设备回读、连接情况和旧动作类型均不决定或替换该草稿。
- 只有显式保存到本机成功后，草稿才落盘并具备运行时转发及启用心跳的条件；设备在线、权限和暂停状态的检查仍然适用。整个过程不写硬件键位表。
- 配置页提供可选的“从设备键位导入”，校验完整六控件只读快照后才载入草稿；不支持的动作会指出具体控件，并保留原草稿。导入不会自动保存或激活结果。
- 已有有效本机文件优先；损坏或不支持的本机文件继续保留并阻止覆盖，须修复后重启应用，不能混淆文件缺失与读取失败。

用户已明确保留双击与长按动作，取消通过设备自动跨 Mac 同步。采用导出、导入完整版本 2 `HostProfile` JSON 的搬移方式，包含主要动作、双击、长按动作及时间参数，再在目标 Mac 显式保存到本机；此流程不将配置同步到设备，也不改写设备键位。

[CONFIRMED] 独立默认草稿版本已通过 143 项原生测试、`make build` 钥匙串证书签名构建及严格签名验证。设备休眠时，实际应用仅显示一个主窗口，六个默认控件均可编辑，未出现 `type=0x03` 初始化错误；给双击草稿分配 Enter 后界面显示 `双击 Enter`，证明离线草稿编辑可用。随后已撤销该验证编辑：双击恢复关闭，撤销按钮禁用，并选回单击；当前保留默认六键待保存，实际本机配置文件仍不存在，未写设备键位。实体输出、手势效果、设备在线且保留旧 `type=0x03` 映射时的运行，以及运行中权限撤销仍未验证。


### 9.9 合成 Fn 反馈污染 HID 状态

最初反馈时默认草稿尚未保存，因此首先指出了配置生效边界。用户随后自行保存 `host-keymap.json`，已保存的顶部按键仅为单击 Fn，没有双击或长按配置；后续故障不能再归因于未保存草稿或可选手势时序。

[CONFIRMED] 用户报告 Fn 仅首次生效，持续按住时输入法也只响应一下。日志显示多组 AU05 按下/松开准确到达，但生成的虚拟键码 63、事件类型 12（`flagsChanged`）在按下和松开时均为 flags 545259520（`0x20800000`）；松开事件仍保留 Fn 位，没有清除。没有证据显示手势路由提前发出结束动作。

[CONFIRMED] 在原进程 PID 86081 的发送器读取修饰键状态前设置断点，首次按下前测得 HID flags 为 0、键码 63 状态为假；其松开前 HID flags 为 545259520、键码 63 状态为真，随后一组按下与松开前也均为真。这证明本应用合成 Fn 反馈进入 `.hidSystemState` 的标志及按键状态；私有事件源不能隔离该汇总，仅从 flags 换成 key state 也无法解决已观察到的污染。

第一次修复引入 `PhysicalFnMonitor`，通过 session 层 listen-only `flagsChanged` tap，仅接受来源 PID 0、键码 63 的更新；但仍在首次合成事件前从全局按键状态取一次初始种子。发送器清除汇总 Fn 位，再合并跟踪状态与软件持有的 Fn，旧原生后端也采用相同规则。

[CONFIRMED] 该中间版本通过 150 项原生测试、钥匙串证书签名的 `make build` 及严格签名验证。PID 91133 保留用户已保存配置，界面显示保存的 Fn 动作及持续保持说明；但实测松开事件仍为 flags `0x20800000`。监控日志显示 Fn 事件来自 PID 91133，过滤器已正确忽略；初始种子却已经继承了旧进程留下的全局 Fn=true 污染，原测试未覆盖该启动状态。因此把汇总读取缩减为一次初始种子仍是失败的中间修复。

第二次纠正删除所有全局初始按键状态读取。`PhysicalFnMonitor` 初值为假，仅在符合来源 PID 0、键码 63 的 `flagsChanged` 事件到达时改变。`VendorKeyBridge` 在 synchronize/pump 启用转发前准备监听，准备失败时保持转发关闭并明确报错；旧 `NativeFnBackend.open` 也在发送事件前启动监控。发送器清除汇总 Fn 位，再合并独立监控状态与软件持有的 Fn；其他修饰键继续使用原有全局状态路径。tap 创建失败或端口失效明确报错，被禁用时尝试重新启用，析构时移除、释放 run-loop source 与 tap。

若监控启动时真实 Fn 已经按住，其初值在符合条件的变化到来之前不可观测，实现暂按假处理。真实 Mac Fn 的来源 PID 0 及物理按键交错尚未实测；模拟状态及交错测试不能证明所有真实键盘组合。

界面解释动作语义：只有单击 Fn 时保持至松开；长按 Fn 超过阈值后开始保持，松开时结束；单击 Fn 与其他手势并存、或双击 Fn 时，手势判定后执行脉冲。该说明不改变此次故障涉及的已确认纯单击配置。

[CONFIRMED] 第二次纠正的 151 项原生测试全部通过，`make build` 已成功，沿用原 Apple Development 证书签名。用户随后报告 `好用了`，确认当前默认 Fn 映射的保持与松开在实际使用中可用。这是用户现场报告，没有采集新版原始 flags 日志来独立核验每条输出边沿；它不证明所有手势映射、指定次数的重复按住序列、真实 Mac Fn 来源 PID 0 或真实 Fn 交错，这些仍未验证。


### 9.10 只读电量状态与主窗口显示

[CONFIRMED] 23:25:12，只读电量请求 `01 01 02 01` 收到明文前缀 `81 01 02 11 f6 0c 0a 00 c8 01 01 00 ...`；同时在线回复前缀为 `06 03 0a 11 01 ...`，确认该样本采集时设备在线。小端电压字节 `f6 0c` 对应 3318 mV。

[CONFIRMED] Studio 的 `kwdm.dylib` 电量回复处理器将 payload 第 0–1 字节按小端读为 `voltage`，第 2–3 字节按小端读为 `battery`，第 6 字节读为 `charging`；本样本分别为 3318、10 和 1，payload 从明文第 4 字节开始。Studio 界面读取 `battery` 整数，经 `deviceBatteryUpdated` 传入 `IconDrawer::setIconForLabel`，上限限制为 100，并按 10/25/50/75 分档图标。[反汇编摘录](evidence/2026-09-21-battery-disassembly.log) 确认本样本报告 10%、充电中、电压 3318 mV，百分比不是由电压推算。

主窗口实现会在连接状态下显示电量，20% 及以下用橙色标注，在线时每 20 秒刷新；读数缺失或失败显示 `电量 —`，仅百分比未知而电压有效时可显示电压。读取错误与更新时间独立于输入运行时，失败会清除旧值，不会停用 Fn。[CONFIRMED] 23:32:22，158 项原生测试全部通过；显式选用原 Apple Development 签名身份的 `make build` 成功，严格代码签名验证通过。真实应用的辅助功能界面树显示 `Vibe Key 已连接 电池电量：10% · 充电中`，截图确认右上方橙色读数完整、没有截断。替身时钟集成测试确认 20 秒调度及电量失败不停止 Fn，该测试覆盖不能证明已经在实机上精确计时验证了 20 秒间隔。用户随后对默认 Fn 保持与松开的确认见 §9.9，本次电量检查没有新增 Fn 事件日志或物理交错证据。

[CONFIRMED] 为下一轮界面更新退出电量版本前，再次检查同一运行应用的辅助功能界面树，已显示 `20% · 充电中`，此前为 `10% · 充电中`。这确认真实运行期间可见电量读数已经刷新；未对两次观察的间隔计时，不能据此验证精确 20 秒周期。

### 9.11 设备示意图与手势卡片

[CONFIRMED] 本轮界面修改已通过 `make build`、Apple Development 证书签名及严格签名验证，最新构建日志为 `/tmp/olanzi-gesture-cards-signed.log`。真实应用截图确认：设备示意图居中，醒目的左右旋箭头位于旋钮两侧，三个按键和旋钮按下各有独立卡片。每个可按压控件的卡片同时显示单击、双击、长按分配；左右旋转每个方向仍仅有一个动作。临时验证草稿已撤销，未保存。本轮界面检查不增加实体手势输出的验证结论。
