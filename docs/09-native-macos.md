# 09 · Native macOS App

> 🌐 [中文](09-native-macos.zh.md)

> 📚 Docs set: [README](../README.md) · [01 Scope](01-ulanzi-studio-scope.md) · [02 Protocol](02-vibekey-protocol.md) · [03 Tool Manual](03-tool-manual.md) · [04 Methodology](04-methodology.md) · [05 Verification Log](05-verification-log.md) · [06 Legacy Prototype](06-local-workspace.md) · [07 Heartbeat](07-heartbeat-investigation.md) · [08 Mac Fn](08-mac-fn.md) · **09 Native macOS** · [10 Input Runtime](10-input-runtime.md)

## 1. Current product entry point

Olanzi's main application is a native macOS menu-bar app. It uses a complete SwiftUI interface for key configuration and AppKit for the menu-bar lifecycle, with device communication implemented in Swift through IOKit and CoreGraphics. Running the app requires no Python process, browser page, or local HTTP server.

The product remains a lightweight Studio alternative with room for additional device modules. Its initial scope is the AU05's three keys and three knob actions, background heartbeats, and host-side actions through the saved host map for ordinary keys and Fn, with optional double-press and long-press gestures. Additional firmware functions require their own protocol validation; the presence of a command in the reverse-engineered table does not make it safe to expose.

| Component | Role |
|---|---|
| `native/Package.swift` | Swift package; macOS 14 minimum, Swift 6 toolchain, Swift 5 language mode |
| `native/Sources/OlanziCore/` | TEA framing, IOKit transport, device state, CoreGraphics Fn events, dedicated background thread |
| `native/Sources/OlanziApp/` | SwiftUI keymap window, application state, AppKit menu bar |
| `native/Tests/` | Native core regression tests |
| `tools/build-macos.sh` | Builds and packages `build/Olanzi.app`; debug by default |
| Legacy Python and `web/` sources | Retained prototype and research tools; not the native app runtime |

## 2. Build and launch

Use macOS 14 or later with a Swift 6 toolchain. From the repository root:

```bash
make dev
```

`make dev` builds and opens the Debug app bundle at `build/Olanzi.app`. Launch the app bundle to use the native window and menu bar. No Python daemon command or browser URL is part of this startup path.

Other targets: `make build` builds Debug without launching, `make release` builds and signs Release and packages a DMG without launching, `make test` runs Swift tests, and `make check-docs` checks bilingual documentation. Both configurations share the `build/Olanzi.app` output path. If Olanzi is already running, quit it from the menu bar before running `make dev` to ensure the newly built version starts; `open` does not restart an existing process.

The build automatically uses the sole valid code-signing identity in your Keychain. If several exist, select one explicitly; if none exist, the build warns and falls back to ad-hoc signing. Set the same identity for Debug and Release to keep the signing requirement stable across rebuilds. A local self-signed certificate supports local development; it is not an Apple Developer distribution certificate or notarization.

```bash
make dev OLANZI_SIGNING_IDENTITY="Digger Local Signing"
```

The variable also accepts a certificate SHA-1 hash, or `-` to explicitly request ad-hoc signing. Signing uses the Keychain private key without exporting it. macOS may ask you to allow `codesign` to access that key.

To build the signed Release app and an installable disk image in one step:

```bash
make release
# Equivalent alias
make dmg
# Explicit signing identity
make release OLANZI_SIGNING_IDENTITY="Digger Local Signing"
```

The outputs are `build/Olanzi.app` and `build/Olanzi-<version>-<arch>.dmg` (currently `Olanzi-0.1.0-arm64.dmg` on Apple Silicon). The architecture comes from the compiled executable; this does not create a universal binary. The DMG contains the app and an Applications shortcut. Open it, drag Olanzi into Applications, then launch that installed copy. Quit a running copy before replacing it.

`tools/package-dmg.sh` packages the existing bundle; normally use `make release` so it is rebuilt first. Packaging signs the DMG with the app's certificate, checks the image checksum, mounts it read-only, verifies the bundled signature and file contents, checks the Applications link, and unmounts it before publishing the output. Temporary files are cleaned up; if unmounting fails, they are retained for recovery. No private key is exported. Local certificate signing is not Apple notarization.

For isolated UI development without real hardware:

```bash
swift run --package-path native Olanzi --demo
```

Demo mode must remain visibly distinguishable from a real device session. It uses simulated device behavior and does not open real HID interfaces or inject system Fn events. A successful demo preview is not evidence of real-device writes or OS actions.

Before connecting real hardware, quit official Studio and stop any legacy tool that still holds the device. The native app does not kill those processes on your behalf. Plug in the receiver, turn on Vibe Key, and check the app's connection state; receiver presence and device-online status are different observations.

## 3. Window and menu-bar lifecycle

The red close button and `Cmd-W` only hide the configuration window and retain drafts. Olanzi keeps running in the menu bar, and its Dock icon remains if already shown. Its background device worker continues the connection, heartbeat scheduling, and ordinary-key and Fn forwarding through the saved host action map while the device is available. Reopen the configuration window from the menu bar or Dock when you need to edit mappings. While a receiver is connected, the background worker holds an activity assertion against App Nap so hiding the window does not reduce heartbeat and input scheduling. It releases the assertion on disconnect or quit, and demo mode never acquires one. This assertion allows the Mac to sleep normally.

Open Settings in the main window’s top navigation, or choose Settings in the macOS application menu (`Cmd-,`), to change the interface language. Options are Follow System (the default), English, and Simplified Chinese; unsupported system languages fall back to English. The selected preference is saved and takes effect immediately without restarting the app. Changing language preserves key mappings and unsaved drafts. User-defined profile names and file paths are not translated. The permission welcome page provides the same language selector, so language can be changed before granting permissions while the rest of the workspace remains gated.

Choose Quit from the application menu to stop the app and device worker. Shutdown attempts to release any synthetic Fn hold and closes the device interfaces. Closing a window is therefore different from quitting the app. Computer sleep interrupts continued heartbeat delivery regardless of whether the window is open.

No login item, `launchd` service, or automatic startup at boot is installed. Start the app manually when needed. Its menu-bar presence provides the background lifetime; a separate Python daemon is unnecessary.

## 4. Host actions and heartbeat

1. Load the existing host configuration first. If its file is missing, the app provides a factory-key draft without reading device mappings as its source or writing a file automatically. Once permissions are ready, this draft is editable even without a connected device. Existing device actions do not block editing. A corrupt local file is preserved and requires repair followed by an app restart.
2. Select an action around the centered device illustration. Prominent left/right arrows beside the knob each edit one rotation action. Each of the three keys has its own separated card on the right; knob press has a card on the left. Each card shows primary, double-press, and long-press labels above their assigned actions. Click an action to edit that control and gesture directly.
3. Review the selected action. Leaving optional double/long gestures unset preserves immediate primary-key response.
4. Apply to this Mac. The worker validates and atomically saves the complete map before activating it. This operation does not write device keycodes.

Selecting a long-press action shows three compact inline options: **Hold**, **Tap Once**, and **Repeat**. Hold starts the action at the threshold and keeps it down until physical release; Tap Once emits one down/up pulse; Repeat emits a configured number of pulses. Only Repeat displays an inline count stepper, defaulting to 2 and bounded to 2–20. Each repeat holds the action for 40 ms, then waits 80 ms after release before the next press; a delayed worker extends these intervals rather than catching up with a rapid sequence. After a repeat group starts, releasing the control still lets the group finish; keeping it held does not trigger another group. Disconnect, sleep, permission loss, or a configuration change cancels the remaining pulses and releases any active action. Hold is the default for profiles without a mode; existing Hold and Tap Once profiles remain compatible, and a missing repeat count defaults to 2. All three options can be clicked directly within the main window, without opening a menu. They appear only for long-press actions and does not apply to rotary controls. Mode and count remain draft edits until saved to this Mac.

The device illustration and action cards are the control selector; there is no repeated control list. Unset optional gestures show `未设置`, and `清除动作` removes the selected optional gesture. Timing and Fn explanations are in the question-mark help. The footer shows pending changes or a save in progress, without a routine saved-state label; errors remain visible. The device page keeps connection help collapsed and omits heartbeat counters.

To assign a key combination, select a control and action, then click **Record Shortcut** beside the action title. A compact recording bar appears beside the action title and captures keys in the current app window. It displays the full combination in place; scroll horizontally or hover to inspect a long combination. Record-again, cancel, and use controls sit on the right. The keyboard remains visible below and is disabled during recording. Hold all keys you want to combine, such as A+B or Control+A+B, then release every key to finish and preview the combination. Modifier-only combinations and Fn can also be recorded; select Caps Lock from the existing keyboard list. Left and right modifiers are normalized to their left-side forms. **Record Again** starts another attempt, **Cancel** discards the recording, and **Use Shortcut** puts the result in the draft; save it to this Mac to activate it. Switching controls, actions, or pages, or leaving the app, stops capture. These combinations hold keys together; they are not timed macros. macOS may intercept system shortcuts before they reach the recorder.

The host map is authoritative for all six controls while Olanzi is running, including ordinary keys and Fn. An existing map or a new default draft can be edited offline; a new draft becomes active only after an explicit successful save. Device refresh and reconnection do not overwrite it. Saved and exported version-2 `HostProfile` JSON includes primary, double-press, and long-press actions, the long-press trigger behavior and repeat count, and timing values. Double/long gestures remain supported; moving the complete configuration between Macs uses export on one Mac and import followed by an explicit local save on the other. Automatic cross-Mac synchronization through the device is not provided. Old six-key native profiles migrate into primary actions. See [10 · Daemon Input Runtime](10-input-runtime.md) for ownership, timing, and migration details.

The profile page offers `从设备键位导入` (import device mappings) as an optional action. It validates the six-control read-only snapshot before loading a draft. An unsupported action identifies the affected control and leaves the current draft unchanged. Importing does not save or activate the draft. The on-device key table remains an independent fallback. Explicit hardware writes through the protocol tool still require ACK/readback and can partially succeed; the native host-action editor no longer invokes that operation. Drafts are separate from both the saved host map and hardware snapshot.

The heartbeat follows Studio's recovered plaintext prefix `06 01 23 00 01` with zero padding. Because it selects vendor-event forwarding, it is enabled only with a successfully saved valid host map, a connected and online device, Input Monitoring and Accessibility permissions, and unsuspended input. Otherwise heartbeats stop while read-only online and key queries remain available; query waits also check permissions and sleep state. Successful transmission does not alone prove radio delivery or idle wakefulness. See [07 · Heartbeat Investigation](07-heartbeat-investigation.md).

The main window displays battery status below the connection state at the top right. While the device is online, the worker sends the read-only `01 01 02 01` query every 20 seconds and records the latest successful timestamp. The UI shows percentage and charging status, with levels at or below 20% in orange. Offline, unknown, or failed readings show `电量 —`; if only percentage is unavailable, a valid voltage is shown instead. The tooltip gives voltage, update time, or an independent read error. Battery failures clear the stale reading and retry on the next interval without disabling Fn forwarding. Decoding is based on [02 · Protocol](02-vibekey-protocol.md#battery-reply-fields); the current sample and verification boundary are in [05 §9.10](05-verification-log.md#910-read-only-battery-status-and-main-window-display).

## 5. Vendor Key Forwarding, Fn, and Permissions

[CONFIRMED] The physical comparison on 2026-09-21 found that one-second Studio heartbeats from Olanzi stopped ordinary-key standard HID output and left the Fn standard-input callback without reports. Closing only the Fn input interface did not help; stopping only the heartbeat while retaining the vendor connection and read-only queries restored Enter. Keys then appeared as `8b 10` vendor events; see [07 §4](07-heartbeat-investigation.md). Both ordinary keys and Fn therefore require application-side forwarding and its permissions.

`GestureRouter` interprets the event's physical control index using the saved host map; `VendorKeyBridge` executes the selected action to emit ordinary keys or Fn. The logical action number in `frame[2]` is not a HID keycode and must not be injected directly; the AU05 physical index is in `frame[4]`. Unapplied drafts do not participate in forwarding. Unknown or multimedia mappings must produce explicit errors rather than guessed input. Physical capture has confirmed all six control indices and rotation directions; rotation indices 4/5 report only down, requiring a host-supplied release pulse. The preceding vendor-routing implementation passed 68 automated tests and a signed Release build; the host-runtime checks are described in [10](10-input-runtime.md). That earlier build confirmed physical Enter and top-key Fn effects. A later knob-to-F13 test confirmed the current Raycast script’s input-field focus action; see [10 §4](10-input-runtime.md#4-holds-cancellation-and-failures). These results do not establish every control or application action.

Fn remains an ordinary key name in the modifier category: select it and apply, without a separate feature switch or `macFnEnabled` setting. Device keycode `0x01` means Fn in the host mapping; the factory top key also uses it. Vendor events carry physical control indices, so the old standard-HID prototype's inability to distinguish same-code controls does not directly apply. Removing every Fn assignment stops Fn generation, while ordinary-key forwarding still requires the bridge and permissions.

Give **Olanzi App** both Input Monitoring and Accessibility permission in System Settings → Privacy & Security. These permissions belong to the native bundle, not the legacy terminal process. Outside demo mode, either missing permission shows only a permission welcome page inside the main app window by replacing its content, without opening a popup, another window, a sheet, the configuration workspace, or the settings interface or stacking initialization errors and a footer error bar. The welcome page provides separate Input Monitoring and Accessibility guidance, buttons for the corresponding system settings, and an app-location entry point. This guidance is independent of map initialization and device online status. Both permissions becoming ready automatically opens the workspace; revocation returns to the welcome page. Demo mode bypasses this gate. Automated regression, build, and real-UI verification of the welcome-page gate passed: one Olanzi main window showed the welcome text, both permission states, and settings and app-location controls, without configuration navigation, initialization errors, a save footer, or an additional popup. Clicking requests the selected permission directly on the app main thread before opening the matching System Settings page; the request no longer waits for the device worker or connection. Input Monitoring uses the HID request API, with an event-listening request as a fallback if access remains denied. The app requests one permission at a time, without adding a separate Fn panel. System policy and existing denial records can still prevent a new prompt; opening Settings alone is not evidence that the app was registered. The app checks permissions once per second without prompting, independently of window activation and the device worker. The UI stores these results separately so older device snapshots cannot overwrite newer permission state; changes automatically switch between the welcome page and workspace and ask the device service to recheck. Stopping the app cancels the timer, and demo mode does not poll. If Settings is enabled but the running process still reports unavailable access, first verify the authorized app copy and path; a Settings switch alone does not establish current-process permission. Ordinary-key and Fn forwarding both depend on these permissions; a device mapping does not establish that access is ready or forwarding is active.

If Olanzi is absent from the settings list, click + to add it. The welcome or device page's “Show App Location” button reveals the running bundle in Finder. Grant permission to that copy at a stable path, such as the repository's `build/Olanzi.app` or `~/Applications/Olanzi.app`. Reuse the same signing certificate and bundle identifier across builds. Switching from ad-hoc signing to a certificate changes the signing requirement, so existing permissions may need to be removed and granted again once. Stable signing does not grant permissions automatically. [CONFIRMED] `codesign` verification of this local build identified the sole Apple Development identity in the keychain and bundle identifier `com.mrcroxx.olanzi`, with no ad-hoc signing. Missing permissions still require user approval in System Settings.

Input Monitoring first reads `CGPreflightListenEventAccess()`. If it is false, a temporary session-level, listen-only event tap containing only key-down/key-up events checks the current process's keyboard-listening capability. The probe must be both valid and enabled and is immediately invalidated and released, without joining a run loop or collecting keyboard content. `IOHIDCheckAccess()` and event-synthesis preflight remain diagnostic values. Accessibility independently reads `AXIsProcessTrusted()` on every check, without short-circuiting on other preflights. Diagnostics include `ListenProbe`, distinguishing probe results from skipped checks. The UI reports current listening capability as `已就绪` (ready), not the separate Settings switch, and does not establish any permission API's caching mechanism. It supersedes the preflight-only decision and conditional AX checks from §9.5/§9.6; see [05 §9.7](05-verification-log.md#97-checking-keyboard-listening-capability-after-a-negative-preflight) for in-process evidence and verification limits.

If migrating from an older ad-hoc build leaves stale denial records, quit Olanzi, reset only its affected permissions, then relaunch the installed copy and click its permission button. This removes existing approvals for those services, which must be granted again. The app never runs these reset commands automatically.

```bash
tccutil reset Accessibility com.mrcroxx.olanzi
tccutil reset PostEvent com.mrcroxx.olanzi
tccutil reset ListenEvent com.mrcroxx.olanzi
open /Applications/Olanzi.app
```


The new forwarding path consumes key events from the existing vendor connection instead of relying on the silent Fn standard-input callback. All key forwarding requires a running app, an online device, a valid saved host map, and the relevant permissions. Closing the window keeps it running; quitting stops it. The implementation must track holds by physical control, avoid duplicate presses, and attempt releases on key-up, disconnect, or quit; Enter and one top-key Fn press/release pair have earlier physical confirmation, which does not establish physical coverage of every control or failure-release scenario.

Fn conversion is not a native firmware Fn command, and neither the protocol nor a simulated event test proves equivalence to an Apple hardware Fn/Globe key. Dictation, input-source switching, Studio-specific Fn handling, and application-specific actions still require physical testing. See [08 · Mac Fn](08-mac-fn.md) for the trigger mechanism and historical prototype implementation; its Python commands and HTTP settings do not apply to the native app.

The native bridge posts Fn events at `cghidEventTap`, retaining a private event source and paired `flagsChanged` events. [CONFIRMED] Inspection of the running Doubao input method found its listener at the HID tap; Studio's general keyboard emitter also posts there. The previous session-level injection bypassed listeners at that earlier stage. Moving injection to the HID tap corrects the routing mismatch; an earlier physical test confirmed top-key Fn activation of Doubao and a cleared Fn bit on release, within the historical test below. That addresses only the injection-stage mismatch. The new heartbeat comparison additionally establishes a standard-HID-to-vendor-event transition, which a Fn tap change alone cannot fix. Ordinary-key and Fn forwarding still use a private CoreGraphics source and the HID tap. A private source does not isolate `.hidSystemState`: later in-process measurements found that both `flagsState` and `keyState` included the app’s synthetic Fn feedback. `PhysicalFnMonitor` now tracks physical Fn and left/right Control, Shift, Option, and Command through a session-level, listen-only `flagsChanged` tap. It accepts only source PID 0 and updates only the side identified by that event’s virtual keycode, avoiding synthetic feedback in unrelated flag bits. The cache starts empty without aggregate-state seeding; `VendorKeyBridge` prepares monitoring before forwarding and stops with an error if preparation fails. In the main path, `MacKeyEmitter` removes these modifiers’ family and side bits from aggregate flags, then merges the monitored state and app-owned holds. Other state, such as Caps Lock, retains its existing handling. The legacy native backend still uses the monitor’s Fn state. A disabled tap clears the untrusted cache before attempting re-enabling; it does not indefinitely pause forwarding while waiting for a physical key event. The next physical side-key event rebuilds that side’s state. Physical modifiers already held before startup or during a missed-event interval must be released and pressed again. The monitor reads modifier metadata only and releases its run-loop source and tap on destruction. Logs at 00:42:57/59 on 2026-09-22 observed one physical Fn press/release pair from PID 0, establishing that sample rather than every keyboard-overlap case; see the [modifier-feedback excerpt](evidence/2026-09-22-modifier-feedback.log). The earlier Fn-only revision is recorded in [05 §9.9](05-verification-log.md#99-synthetic-fn-feedback-contaminating-the-hid-state); the two post-fix physical trials and their limits are recorded below.

For a sustained Fn hold, assign Fn only to the primary action and disable optional gestures: it stays held until release. A long-press Fn action set to Hold begins holding after its threshold and ends on release; Tap Once emits a single Fn pulse at the threshold, while Repeat emits the configured number of Fn pulses; neither repeats a group merely because the control remains held. Primary Fn combined with other gestures, and double-press Fn, produce a pulse after gesture recognition; the UI explains this distinction. These are the runtime’s action semantics, not proof of an input method’s response.

Local diagnosis uses the `com.mrcroxx.olanzi` log subsystem's `device-input` category. Distinguish vendor frames, decoded physical-control press/release events, saved host actions, and host event emission. An empty standard-input callback is no longer evidence that no key event exists. The [vendor key routing record](evidence/2026-09-21-vendor-key-routing.log) retains the six-control events and Enter / Fn validation excerpts. The six-control capture establishes indices and edge shapes; that historical sample additionally confirms physical OS effects for Enter and top-key Fn. The later [F13 verification log](evidence/2026-09-22-f13-hotkey.log) records knob-triggered Raycast script runs and the user’s input-focus confirmation. A pending draft is not the device mapping; save to this Mac and wait for activation before testing.

## 6. Legacy tools and migration boundary

The Python files, browser interface, and daemon manager remain available as the earlier prototype described in [06 · Legacy Local Workspace](06-local-workspace.md). The single-file `vibekey.py` remains a reverse-engineering and terminal inspection tool documented in [03 · Tool Manual](03-tool-manual.md). Their permissions and runtime instructions apply only when deliberately running those tools.

The native app does not require the legacy HTTP service to be running. Avoid running both against the same receiver, because an occupied interface can prevent connection. A previously running legacy service is not automatically stopped or migrated by launching the app. Native local settings, legacy JSON settings, and browser-local profiles are separate stores; the legacy Fn switch does not affect the native app, whose Fn behavior follows its saved host action map.

## 7. Verification and remaining observations

Run the native tests and documentation checks from the repository root:

```bash
swift test --package-path native
python3 tools/check_docs.py
git diff --check
```

Python in the documentation-check command is a repository maintenance tool, not a native runtime requirement. Native automated tests exercise protocol and state behavior with controlled inputs; a successful build or demo establishes neither a physical Fn action nor idle-sleep prevention.

The preceding vendor-routing implementation passed 68 automated tests and a signed Release build; host-runtime checks are described in [10](10-input-runtime.md). Earlier demo UI checks covered selection through the knob-side rotation arrows, Fn assignment, apply/readback, profile saving, reopening the window, and graceful quit; those demo checks did not open real hardware.

[CONFIRMED] In the earlier vendor-routing build, the user confirmed Enter newline and top-key Fn activation of Doubao, reporting `好用！！`. Logs record Enter virtual keycode 36 down/up and one Fn virtual keycode 63 pair: down at 17:31:24.285 with flags 545259520, then up at 17:31:25.616 with flags 536870912, clearing the Fn bit. See the [verification excerpts](evidence/2026-09-21-vendor-key-routing.log). This establishes neither two Fn trials nor successful OS actions for all six controls.

[CONFIRMED] The independent-default-draft revision passed all 143 native tests, `make build` with keychain-certificate signing, and strict signature verification. With the device asleep, the actual app showed one main window with all six default controls editable and no `type=0x03` initialization error. Assigning Enter to a double-press draft displayed `双击 Enter`, establishing offline draft editing. The test edit was then discarded: double press returned to disabled, the undo button became disabled, and the primary action was selected again. At that offline-UI checkpoint, the six-key default draft remained pending save, and the real host configuration file still did not exist; the user subsequently saved it before the investigation in [05 §9.9](05-verification-log.md#99-synthetic-fn-feedback-contaminating-the-hid-state). No device mappings were written. Physical output, gesture effects, online operation with the legacy `type=0x03` mapping, and running-process permission revocation remain unverified.

[CONFIRMED] After the user saved the host map with primary-only Fn and no optional gestures, repeated AU05 press/release edges arrived correctly, but generated Fn releases retained the Fn bit. In-process inspection showed synthetic Fn feedback in both HID flags and key state; this was not evidence of an early gesture-router release. The first monitor attempt passed 150 tests and a signed build, but PID 91133 still emitted the Fn bit on release: its one-time global key-state seed inherited contamination from the previous process even though it correctly ignored its own PID events. The second revision removes that seed and starts monitoring before forwarding; all 151 native tests passed. `make build` succeeded with the existing Apple Development certificate; the user subsequently reported `好用了`, confirming that the default Fn mapping can be held and released in actual use. No new-build raw flags log was captured. That checkpoint did not verify all gestures, a counted repeated-hold sequence, actual Mac Fn source PID 0, or real-Fn overlap; the later PID 0 sample is documented above. Initial physical Fn state is unobservable if the key was already held when monitoring began. See [05 §9.9](05-verification-log.md#99-synthetic-fn-feedback-contaminating-the-hid-state).

[CONFIRMED] A later intermittent failure was reproduced on 2026-09-22: the final Command release in a Command+Enter action at 00:45:18 retained the Command bit (`537919496` / `0x20100008`). The following Fn press at 00:45:20 carried both Fn and Command (`546308104` / `0x20900008`); its release at 00:45:22 cleared Fn but retained Command. The user reported that pressing the physical keyboard Fn once restored AU05 Fn behavior. Three earlier successful trials had not reproduced the defect and do not establish that it was absent. The emitted flags confirm residual Command; the affected code read aggregate system modifier state, which can include the app’s own synthetic events. Subsequent post-fix trials are documented below. See the [modifier-feedback excerpt](evidence/2026-09-22-modifier-feedback.log).

[CONFIRMED] The earlier Hold/Tap Once and modifier-feedback revisions passed 212 native, 84 Python, and 3 JavaScript tests, plus a Debug build signed with the existing Keychain certificate and strict signature verification. Real UI checks in Chinese and English confirmed the long-press selector layout, draft-only changes to Tap Once, undo restoring Hold, no selector for either rotation direction, and availability for knob press. The saved configuration’s SHA-256 remained unchanged. Deterministic modifier-feedback regressions pass; physical long-press Tap Once behavior remains unverified; the subsequent modifier/Fn trials are recorded below. Physical modifiers already held before monitoring starts or during a monitor interruption must be released and pressed again.

[CONFIRMED] The user subsequently reported `没问题！` after two trials on the new build, process 28468, at 00:57:00–07 on 2026-09-22. Each Command+Enter action ended with Command cleared (`536870912` / `0x20000000`), followed by Fn down with `545259520` / `0x20800000` and Fn up with `536870912` / `0x20000000`, without residual Command. The recorded Fn holds lasted approximately 1.1 s and 0.87 s. This verifies those two sequences; the earlier failure and the post-fix raw events are both preserved in the [modifier-feedback log](evidence/2026-09-22-modifier-feedback.log). Physical long-press Tap Once, Repeat, and broader physical-keyboard overlap/interruption scenarios remain unverified.

For hardware validation, record the actual on-device readback separately from the visible OS action. In particular, test a one-second Fn hold and release with a two-second pause, confirm no repeated presses, then check the intended action in the target application. Distinguish the app's event state from macOS behavior. Long-idle heartbeat tests must run for the relevant duration with the app still running. Do not mark these observations confirmed until direct evidence exists.
