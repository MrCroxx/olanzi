# 10 · Daemon Input Runtime

> 🌐 [中文](10-input-runtime.zh.md)

> 📚 Docs set: [README](../README.md) · [02 Protocol](02-vibekey-protocol.md) · [07 Heartbeat](07-heartbeat-investigation.md) · [09 Native macOS](09-native-macos.md) · **10 Input Runtime**

## 1. Ownership and scope

All six controls enter one host input pipeline. The menu-bar application's device worker owns the receiver, heartbeat, gesture clock, committed action map, and generated key holds. The window edits drafts and submits configurations; closing it does not stop the worker. This is an in-process background service, not a separately installed launch daemon or login item. Quitting the app stops forwarding.

The previous implementation looked up each event in the device's key table. That conflated the hardware fallback with runtime actions and had no place to recognize double presses or long presses. The runtime now loads the persistent host configuration first. A missing file makes the UI offer its own default draft; the worker does not seed from device mappings or write a file automatically. Device snapshots remain independent, and refreshes and reconnects do not replace the host configuration or draft. Normal edits never issue a hardware key-table write.

| Layer | Responsibility |
|---|---|
| `MacHIDTransport` | Decode vendor frames and deliver physical-control edges, including while a query is waiting |
| `GestureRouter` | Interpret edges and monotonic deadlines; choose exactly one configured action |
| `VendorKeyBridge` | Execute action holds, preserve shared modifiers, and retry failed releases |
| `MacKeyEmitter` | Translate keyboard usages to macOS events and post at the HID tap |
| `PhysicalFnMonitor` | Track physical Fn metadata independently of aggregate synthetic-event feedback |
| `HostKeymapStore` | Validate and atomically persist the authoritative host configuration |
| SwiftUI / `AppModel` | Edit drafts, choose gestures, import/export profiles, and submit saves |

Vendor events identify controls independently of their configured actions. The AU05 physical index is `frame[4]`; `frame[2]` is a logical action number and is never injected as a keyboard usage. The four push controls send press/release edges. Rotary controls 4 and 5 send a press-only pulse per tick. See [the routing capture](evidence/2026-09-21-vendor-key-routing.log).

## 2. Actions and timing

Each control has a primary `press` action. The four push controls may additionally configure `doublePress` and `longPress`. Each action contains keyboard entries. The keyboard picker assigns one key to the chosen action, while the compact recorder assigns combinations that can include multiple ordinary keys and modifiers. Long-press actions also have a `longPressBehavior`: `hold` (default) keeps the action down from its threshold until physical release, while `tap` emits one down/up pulse at the threshold without repeating while the control stays down. The compact Trigger Behavior selector, shown only when editing long press, labels these options Hold and Tap Once. An absent optional action disables that gesture, while a configured unassigned key is an intentional no-op. Rotary controls reject double/long mappings.

The editor places distinct gesture cards around a centered device illustration: one card for each of the three keys on the right and one for knob press on the left. Each card has three columns with the gesture name above its action; clicking an action selects that control and gesture. Left/right rotation arrows flank the knob and each expose only a primary action. There is no duplicate control list. Detailed timing and Fn explanations are available through question-mark help; normal operation omits saved-state labels and heartbeat counters while keeping errors visible.

| Configuration or event | Behavior |
|---|---|
| No optional gesture | Primary down is immediate; primary up follows physical release. Fn and modifiers acquire no gesture delay. |
| Long press only | Release before the threshold emits one primary pulse; reaching the threshold executes the long action using Hold or Tap Once. |
| Double press configured | The first release waits for the double-press window; expiry emits one primary pulse. |
| Second down before the double deadline | Claims the second press; its release emits one double action pulse. |
| Second press reaches the long threshold | A configured long action takes precedence; neither the pending primary nor double action is emitted. |
| Second down exactly at or after the deadline | The first primary expires first; the new down starts another gesture. |
| Rotary tick | Emit one primary down/up pair immediately, without waiting for a device release. |

The default double-press window is 250 ms from the first release; the long-press threshold is 500 ms from the relevant press. Configuration accepts a double window of 150–500 ms and a long threshold of 300–2000 ms, with the long threshold greater than the double window. The clock is monotonic. Duplicate down packets do not restart a hold or deadline.

Enabling an optional gesture necessarily delays the ambiguous primary action. Keep optional gestures disabled on a control that must immediately act as a held Fn, Shift, or other modifier. A long action mapped to Fn with Hold begins holding Fn only after its threshold and releases when the control is released; Tap Once emits one Fn pulse at the threshold and does not repeat while held. Primary Fn with another gesture enabled, and double-press Fn, are pulses after gesture recognition. The UI explains continuous holds versus pulses; an input method’s actual behavior still requires physical verification.

## 3. Configuration and migration

The active map is stored at `~/Library/Application Support/Olanzi/host-keymap.json`. It contains six unique control indices, the action entries, timing values, and schema version. The store validates before writing, uses an atomic replacement, and does not activate a new map when persistence fails. If the file is missing, the worker publishes `hostConfigurationMissing=true` without creating a map or writing a file. The app offers `HostKeymap.defaultKeymap` as an editable draft, independent of device presence or the types in its old key table. A corrupt or unsupported local file remains an error, requires repair followed by an app restart, and cannot be overwritten by a draft. Only an explicit successful save persists and activates a new host map; until then, the default draft does not enable heartbeats. This process never writes the device key table.

The device table is a separate fallback used when the app is absent and the receiver returns to standard HID output. Editing the fallback remains an explicit protocol-tool operation described in [03 · Tool Manual](03-tool-manual.md). Applying a host map or importing a profile does not rewrite it. The profile page can explicitly import the read-only device key snapshot into a draft. All six controls must validate; a failure identifies the affected control and preserves the existing draft. This optional import is not initialization or activation. Default drafts and existing host maps can be edited and saved offline; demo mode uses memory only and never touches the real host store or device.

Exported `HostProfile` version 2 includes every control’s `press`, optional `doublePress` and `longPress` actions, the `longPressBehavior` value (`hold` or `tap`), and the `doublePressWindow` and `longPressThreshold` timing values. Older profiles without `longPressBehavior` decode as `hold`; rotary controls do not support long-press behavior. Double/long gestures remain supported. The chosen cross-Mac workflow is to export this complete JSON, import it on the destination Mac, and explicitly save it locally. Automatic cross-Mac synchronization through the device is not provided, and profile transfer never writes the device key table. Import accepts legacy native version-1 six-code profiles by turning their codes into primary actions with optional gestures disabled. Saved profile collections use `nativeHostProfiles`; the old `nativeProfiles` collection remains available as the migration source. Entries are decoded and migrated independently, so one corrupt or unsupported entry cannot prevent valid profiles from loading. Complete original data containing failed entries is backed up in `nativeHostProfilesRecoveryBackups`; subsequent profile saves or deletions do not overwrite that recovery copy. Import is bounded to 32 KiB and rejects invalid versions, duplicate/missing controls, unsupported keys, invalid timings, and rotary gesture mappings.

The compact recording bar beside the action title updates the selected action draft through the same host-map workflow. It displays the complete combination in place, with horizontal scrolling or hover for long combinations; the keyboard remains visible below and is disabled during recording. It captures a group of keys held together in the current app window, including A+B, Control+A+B, modifier-only combinations, and Fn. Releasing all keys finishes the group and displays its preview. The recorded group represents simultaneous held keys, not a timed macro. Left and right modifiers are normalized to their left-side forms; Caps Lock remains selectable through the existing keyboard list. **Record Again** replaces the pending recording, and **Use Shortcut** replaces the selected draft action; the map becomes persistent and active only after an explicit local save succeeds. Canceling preserves the draft, and switching controls, actions, or pages, or leaving the app, ends capture. System-reserved shortcuts may not reach the recorder. See [09 · Native macOS App](09-native-macos.md#4-host-actions-and-heartbeat) for the recording steps.

The interface-language preference is independent of the host map and exported profiles. Follow System defaults to English when the system language is unsupported; English and Simplified Chinese can also be selected explicitly. Switching language updates the interface immediately and saves the preference without replacing the active map, discarding draft actions, or translating user-defined profile names and file paths. See [09 · Native macOS App](09-native-macos.md#3-window-and-menu-bar-lifecycle) for the Settings and permission-welcome-page entry points.

Each save has a distinct request ID and returns its matching success or failure in the snapshot. The UI ends its pending-save state only for that request, rather than treating busy-state changes from refresh or reconnection as save completion. Further draft edits made during the save remain intact.

## 4. Holds, cancellation, and failures

An emitted hold owns a snapshot of its action. Shared macOS keys are reference-counted across controls, including aliases such as Print Screen and F13; releasing one control must not release another control's key. Other active modifiers remain present in emitted flags. Every rotary tick receives a host-generated release.

[CONFIRMED] F1–F20 down and up events also carry their own function flag. This shares a bit with physical Fn but does not represent an additional Fn hold. See Apple's [function flag documentation](https://developer.apple.com/documentation/appkit/nsevent/modifierflags-swift.struct/function). Investigation of an F13 hotkey on 2026-09-22 found that the old emitter omitted this bit when overwriting event flags; two new regression tests failed before the fix. The emitter now adds it by macOS virtual key, including Print Screen and other F13–F15 aliases, without registering them as held modifiers. Subsequent ordinary keys and Fn releases still clear synthetic feedback. All 173 tests passed after the fix, and the build was signed with the existing Keychain identity. Physical logs captured F13 events corresponding to knob presses and releases, alongside successful Raycast script runs at matching times. The user confirmed that both attempts focused the input field. This verifies the current script and configuration, not every application hotkey. See the [raw F13 verification log](evidence/2026-09-22-f13-hotkey.log).

`PhysicalFnMonitor` now tracks physical Fn and both sides of Control, Shift, Option, and Command through a session-level, listen-only `flagsChanged` tap. It accepts source PID 0 and updates only the side identified by the event’s virtual keycode, without inheriting unrelated flag bits or seeding from aggregate system state. The cache starts empty. `VendorKeyBridge` prepares monitoring before forwarding; preparation failure disables forwarding with an error. The legacy `NativeFnBackend.open` also starts the monitor before event emission and uses its Fn state. In the main emitter, these modifiers’ family and side bits are removed from aggregate flags before merging the monitored state and app-owned holds; Caps Lock and other state retain their existing handling. When the tap is disabled, its untrusted cache is cleared before re-enabling. Forwarding does not wait indefinitely for physical key input; the next side-key event rebuilds that side’s state. A physical modifier already held at startup or during a missed-event interval must be released and pressed again. Invalid ports and failed creation or re-enabling report errors; destruction removes and releases the run-loop source and tap. The monitor reads modifier metadata, not keyboard text. The log at 00:42:57/59 on 2026-09-22 observes one physical Fn press/release pair from source PID 0, not every keyboard-overlap case; see the [modifier-feedback excerpt](evidence/2026-09-22-modifier-feedback.log). The two post-fix physical trials and their limits are recorded below.

Disconnect, loss of online status or permissions, system sleep, configuration replacement, and shutdown cancel unresolved gestures and attempt to release owned keys. Cancellation never flushes a delayed single action into the newly focused application. Held push controls are quarantined until their physical release after a configuration change, preventing a held key from becoming a new action under the replacement map. A failed release remains pending for retry rather than being treated as successful. Sleep immediately gates input callbacks while the worker cancels gestures and closes the connection; waking reconnects only if the user had not manually disconnected. No pending pre-sleep action is replayed.

Studio heartbeats default to disabled. They are enabled only when a successfully saved valid host map, a connected and online device, Input Monitoring and Accessibility permissions, and unsuspended input are all ready. An unsaved default draft, a local configuration-file error, or missing permissions leaves read-only vendor queries available without continuing to take over device input through heartbeats. Permission diagnostics are independent of map initialization and device online status. Outside demo mode, missing Input Monitoring or Accessibility permission replaces the main app window's content with the permission welcome page and authorization guidance, without opening a popup, another window, or a sheet, or entering the configuration workspace or settings interface. Both permissions becoming ready opens the workspace automatically; revocation returns to the welcome page, without stacking initialization errors and footer notices. Query waits also check suspension conditions. Normal online queries retain the last confirmed state until completion, avoiding cancellation of held actions every two seconds.

The app independently reads `InputPermission.currentStatus()` once per second in the main run loop's common mode, without depending on window activation or completion of device jobs and without requesting authorization. Polled results are stored separately so older device snapshots cannot roll them back; only changes ask the service to recheck immediately and automatically switch the welcome page. Stopping or destroying the model cancels the timer; demo mode does not read real permissions. Input Monitoring first reads the CG listening preflight. Only when false does it create a temporary session-level, listen-only, keyboard-only capability probe, checking that it is valid and enabled before immediately releasing it without joining a run loop or reading events. A true preflight skips the probe. Either successful check means current keyboard-listening capability is `已就绪` (ready), not that the UI mirrors a separate Settings switch. HID and event-synthesis preflight remain diagnostic values. Accessibility reads `AXIsProcessTrusted()` unconditionally on every check, preventing any other preflight from short-circuiting the real AX state. `ListenProbe` diagnostics distinguish probe results from skipped checks.

Device-query waits continue to deliver physical events, so a readback cannot compress an entire Fn hold into two events after the query returns. Gesture deadlines are serviced by the worker; no window timer owns input behavior. Explicit device-map import, profile import, and host-action validation reject unknown action types and unsupported macOS usages instead of guessing input. Unsupported types in an ordinary device readback do not block default drafts or an existing host map. F21–F24 have no supported virtual-key mapping in this implementation.

## 5. Verification and extension boundary

The automated suite exercises deterministic gesture traces, exact deadline boundaries, long/double exclusivity, rotary pulses, cancellation, modifier ownership, configuration validation, persistence failure, migration, and service integration. Tests use fake emitters and temporary stores; passing tests is not evidence of a physical OS shortcut firing. The earlier Enter and Doubao Fn physical evidence in [07](07-heartbeat-investigation.md) establishes the vendor-to-host route, not every new gesture or application action.

Demo UI verification covered assigning and saving double/long actions, displaying both in the device diagram and control list, and restricting rotary controls to immediate actions. Offline editing and save races are covered by model/service tests. New physical double/long shortcuts remain to be verified on the device.

Local review on 2026-09-21 exposed missed cases in heartbeats after unsupported initial mappings, save-completion identification, profile-collection migration, and hidden permission controls before initialization. Before the welcome-page change, the fix baseline passed all 131 native tests, the build, and strict signature verification. With the real device online and initialization failing, heartbeats were confirmed stopped and creating a factory-key draft enabled editing; this check neither saved the host configuration nor rewrote the device. The observed UI combining the initialization error, missing permissions, and authorization button was an intermediate debugging state, subsequently replaced by the permission welcome page at the user's request. All 133 native tests and the build passed after the welcome-page implementation, with keychain-certificate signing retained. Real-UI verification confirmed a complete permission welcome page inside the single main app window, without configuration navigation, initialization errors, a save footer, or an additional popup. Physical forwarding and double/long-press effects after authorization remain pending. See [05 §9](05-verification-log.md#9-native-app-local-review-and-fixes-2026-09-21) for observations and pending checks.

A subsequent fix for the welcome page failing to refresh after authorization adds independent permission polling and current event-listening capability checks; all 136 native tests passed. The build and strict signature verification passed. After rebuilding and restarting, the real welcome page showed Input Monitoring enabled, Accessibility awaiting approval, and the primary button targeting Accessibility settings. A subsequent direct check inside the still-running, originally signed app measured AX trust as true and event-synthesis preflight as false, confirming that the pending Accessibility notice was a code short-circuit error. The check now follows AX trust. All 138 native tests, the keychain-certificate-signed build, and strict signature verification passed; after rebuilding and restarting, the real app entered the main configuration window directly without the welcome page or a missing-permission notice. This records verification before the capability probe was added; the subsequent same-process grant transitions are described below. True AX trust does not establish physical event injection.

The capability-probe version passed all 139 native tests, `make build`, and strict signature verification with the same keychain Apple Development certificate. Real PID 81521 started with both permissions unavailable. The user's Input Monitoring approval automatically updated its checkmark; AX subsequently became true and the app entered the main configuration window without a process change, quit, or restart. CGListen remained false while `ListenProbe` changed from false to true, establishing that the probe drove this listening-capability update. A complete running-process revocation cycle and actual key output remained untested at that checkpoint; the original unsupported device type still blocked initialization in that historical build.

The current architecture removes that device-seeding dependency: a missing host file leads directly to an editable default draft, with device mappings available only through explicit import. This supersedes the earlier initialization rule and its manual factory-draft recovery. All 143 native tests, `make build` with keychain-certificate signing, and strict signature verification passed for this correction. When the new build was launched with the device asleep, its single main window exposed all six default controls for editing without the previous unsupported-type initialization error. Assigning Enter to a double-press draft visibly displayed `双击 Enter`, verifying offline draft editing. The test edit was discarded, double press returned to disabled, the undo button became disabled, and the primary action was selected again. At that offline-UI checkpoint the six-key default draft remained pending save, and the real host file still did not exist; the user subsequently saved it before the Fn investigation below. This check did not write device mappings; physical key output, gesture effects, online operation with the unsupported legacy type, and running-process permission revocation remain unverified. See [05 §9.8](05-verification-log.md#98-separating-host-defaults-from-optional-device-map-import).

[CONFIRMED] After the user saved the host map with primary-only Fn and no optional gestures, repeated AU05 press/release edges arrived correctly, but generated Fn releases retained the Fn bit. In-process inspection showed synthetic Fn feedback in both HID flags and key state; this was not evidence of an early gesture-router release. The first monitor attempt passed 150 tests and a signed build, but PID 91133 still emitted the Fn bit on release: its one-time global key-state seed inherited contamination from the previous process even though it correctly ignored its own PID events. The second revision removes that seed and starts monitoring before forwarding; all 151 native tests passed. `make build` succeeded with the existing Apple Development certificate; the user subsequently reported `好用了`, confirming that the default Fn mapping can be held and released in actual use. No new-build raw flags log was captured. That checkpoint did not verify all gestures, a counted repeated-hold sequence, actual Mac Fn source PID 0, or real-Fn overlap; the later PID 0 sample is documented above. Initial physical Fn state is unobservable if the key was already held when monitoring began. See [05 §9.9](05-verification-log.md#99-synthetic-fn-feedback-contaminating-the-hid-state).

[CONFIRMED] A later intermittent failure was reproduced on 2026-09-22: the final Command release in a Command+Enter action at 00:45:18 retained the Command bit (`537919496` / `0x20100008`). The following Fn press at 00:45:20 carried both Fn and Command (`546308104` / `0x20900008`); its release at 00:45:22 cleared Fn but retained Command. The user reported that pressing the physical keyboard Fn once restored AU05 Fn behavior. Three earlier successful trials had not reproduced the defect and do not establish that it was absent. The emitted flags confirm residual Command; the affected code read aggregate system modifier state, which can include the app’s own synthetic events. Subsequent post-fix trials are documented below. See the [modifier-feedback excerpt](evidence/2026-09-22-modifier-feedback.log).

[CONFIRMED] The long-press behavior and modifier-feedback revisions passed 212 native, 84 Python, and 3 JavaScript tests, plus a Debug build signed with the existing Keychain certificate and strict signature verification. Real UI checks in Chinese and English confirmed the long-press selector layout, draft-only changes to Tap Once, undo restoring Hold, no selector for either rotation direction, and availability for knob press. The saved configuration’s SHA-256 remained unchanged. Deterministic modifier-feedback regressions pass; physical long-press Tap Once behavior remains unverified; the subsequent modifier/Fn trials are recorded below. Physical modifiers already held before monitoring starts or during a monitor interruption must be released and pressed again.

[CONFIRMED] The user subsequently reported `没问题！` after two trials on the new build, process 28468, at 00:57:00–07 on 2026-09-22. Each Command+Enter action ended with Command cleared (`536870912` / `0x20000000`), followed by Fn down with `545259520` / `0x20800000` and Fn up with `536870912` / `0x20000000`, without residual Command. The recorded Fn holds lasted approximately 1.1 s and 0.87 s. This verifies those two sequences; the earlier failure and the post-fix raw events are both preserved in the [modifier-feedback log](evidence/2026-09-22-modifier-feedback.log). Physical long-press Tap Once and broader physical-keyboard overlap/interruption scenarios remain unverified.

Run the checks and build locally:

```bash
make test
make check-docs
make release
```

The PR workflow also checks the legacy protocol suites and builds a DMG with an ad-hoc CI signature. CI artifacts are not notarized releases and do not use the developer's local signing key. Future action types can be added after gesture selection without changing the USB protocol or coupling input behavior to SwiftUI. Macros, application launch actions, and multimedia commands are not implicitly enabled by this design.

[CONFIRMED] On 2026-09-22, shortcut recording passed 192 native tests covering overlapping ordinary keys, modifiers, completion after release, repeat filtering, the 24-entry limit, profile round trips, and simulated event emission. In the app signed with the existing Keychain identity, UI automation verified recording `⌃⌥⌘ I`, `⌘Q`, and `Esc`, confirmation into a draft, and discarding that draft; the saved user configuration was unchanged. An early version rejected modifier notifications without a specific keycode, and some automated input omitted the final modifier-release notification. The recorder now accepts aggregate notifications and checks the four modifier states every 50 milliseconds only during active recording, stopping that check when recording ends. This check does not infer Fn. Deterministic tests cover overlapping `A+B` recording, but this desktop automation did not physically generate it. Recognition of arbitrary ordinary-key combinations still depends on the target application.
